# Optional: direct URL to hosted resume file.
# If empty, the app checks for /resume.pdf in public/.
VITE_RESUME_URL=

# Contact form delivery:
# Option A: full endpoint URL (custom API, webhook, etc.)
VITE_CONTACT_ENDPOINT=

# Option B: Formspree form ID (used only if VITE_CONTACT_ENDPOINT is empty)
VITE_FORMSPREE_ID=

# Optional: GitHub token to reduce API rate limits for GitHub stats.
VITE_GITHUB_TOKEN=

# Optional: custom analytics event ingestion endpoint.
VITE_ANALYTICS_ENDPOINT=
